<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.company.afinievskym.letsdoit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/title"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AddNewTaskActivity" />
        <activity
            android:name=".MainDrawerAndTasks"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListsTask" />
        <activity android:name=".Calendar" />
        <activity
            android:name=".Classes_for_Task.ObjectiveActivity"
            android:label="@string/title_activity_objective"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>